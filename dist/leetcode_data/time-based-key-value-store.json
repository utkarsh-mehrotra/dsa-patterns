{
  "title": "Time Based Key-Value Store",
  "difficulty": "Medium",
  "description": "<p>Design a time-based key-value data structure that can store multiple values for the same key at different time stamps and retrieve the key's value at a certain timestamp.</p>\n<p>Implement the <code>TimeMap</code> class:</p>\n<ul>\n<li><code>TimeMap()</code> Initializes the object of the data structure.</li>\n<li><code>void set(String key, String value, int timestamp)</code> Stores the key <code>key</code> with the value <code>value</code> at the given time <code>timestamp</code>.</li>\n<li><code>String get(String key, int timestamp)</code> Returns a value such that <code>set</code> was called previously, with <code>timestamp_prev &lt;= timestamp</code>. If there are multiple such values, it returns the value associated with the largest <code>timestamp_prev</code>. If there are no values, it returns <code>\"\"</code>.</li>\n</ul>\n<p> </p>\n<p><strong class=\"example\">Example 1:</strong></p>\n<pre>\n<strong>Input</strong>\n[\"TimeMap\", \"set\", \"get\", \"get\", \"set\", \"get\", \"get\"]\n[[], [\"foo\", \"bar\", 1], [\"foo\", 1], [\"foo\", 3], [\"foo\", \"bar2\", 4], [\"foo\", 4], [\"foo\", 5]]\n<strong>Output</strong>\n[null, null, \"bar\", \"bar\", null, \"bar2\", \"bar2\"]\n\n<strong>Explanation</strong>\nTimeMap timeMap = new TimeMap();\ntimeMap.set(\"foo\", \"bar\", 1);  // store the key \"foo\" and value \"bar\" along with timestamp = 1.\ntimeMap.get(\"foo\", 1);         // return \"bar\"\ntimeMap.get(\"foo\", 3);         // return \"bar\", since there is no value corresponding to foo at timestamp 3 and timestamp 2, then the only value is at timestamp 1 is \"bar\".\ntimeMap.set(\"foo\", \"bar2\", 4); // store the key \"foo\" and value \"bar2\" along with timestamp = 4.\ntimeMap.get(\"foo\", 4);         // return \"bar2\"\ntimeMap.get(\"foo\", 5);         // return \"bar2\"\n</pre>\n<p> </p>\n<p><strong>Constraints:</strong></p>\n<ul>\n<li><code>1 &lt;= key.length, value.length &lt;= 100</code></li>\n<li><code>key</code> and <code>value</code> consist of lowercase English letters and digits.</li>\n<li><code>1 &lt;= timestamp &lt;= 10<sup>7</sup></code></li>\n<li>All the timestamps <code>timestamp</code> of <code>set</code> are strictly increasing.</li>\n<li>At most <code>2 * 10<sup>5</sup></code> calls will be made to <code>set</code> and <code>get</code>.</li>\n</ul>\n",
  "examples": [],
  "constraints": [
    "1 <= key.length, value.length <= 100",
    "key and value consist of lowercase English letters and digits.",
    "1 <= timestamp <= 107",
    "All the timestamps timestamp of set are strictly increasing.",
    "At most 2 * 105 calls will be made to set and get."
  ],
  "code_stubs": [
    {
      "language": "C++",
      "lang_slug": "cpp",
      "code": "// --- Helper Parsing Library ---\n#include <iostream>\n#include <vector>\n#include <string>\n#include <sstream>\n#include <queue>\n#include <algorithm>\n\nusing namespace std;\n\n#ifndef CP_STRUCT_DEFS\n#define CP_STRUCT_DEFS\n// ListNode structure\nstruct ListNode {\n    int val;\n    ListNode *next;\n    ListNode() : val(0), next(nullptr) {}\n    ListNode(int x) : val(x), next(nullptr) {}\n    ListNode(int x, ListNode *next) : val(x), next(next) {}\n};\n\n// TreeNode structure\nstruct TreeNode {\n    int val;\n    TreeNode *left;\n    TreeNode *right;\n    TreeNode() : val(0), left(nullptr), right(nullptr) {}\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n    TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}\n};\n#endif\n\n// Clean string helper\ninline string trim(string s) {\n    s.erase(s.begin(), find_if(s.begin(), s.end(), [](unsigned char ch) {\n        return !isspace(ch);\n    }));\n    s.erase(find_if(s.rbegin(), s.rend(), [](unsigned char ch) {\n        return !isspace(ch);\n    }).base(), s.end());\n    return s;\n}\n\ninline vector<string> splitInputs(string line) {\n    vector<string> res;\n    string current = \"\";\n    int brackets = 0;\n    bool inQuotes = false;\n    for (size_t i = 0; i < line.length(); ++i) {\n        char c = line[i];\n        if (c == '\"' && (i == 0 || line[i-1] != '\\\\')) {\n            inQuotes = !inQuotes;\n        }\n        if (!inQuotes) {\n            if (c == '[' || c == '{') brackets++;\n            if (c == ']' || c == '}') brackets--;\n        }\n        if (c == ',' && brackets == 0 && !inQuotes) {\n            res.push_back(current);\n            current = \"\";\n        } else {\n            current += c;\n        }\n    }\n    if (!current.empty()) {\n        res.push_back(current);\n    }\n    for (auto& token : res) {\n        token = trim(token);\n        size_t eqPos = token.find('=');\n        if (eqPos != string::npos) {\n            token = trim(token.substr(eqPos + 1));\n        }\n    }\n    return res;\n}\n\ninline int parseInt(string s) { return s.empty() ? 0 : stoi(trim(s)); }\ninline double parseDouble(string s) { return s.empty() ? 0.0 : stod(trim(s)); }\ninline bool parseBool(string s) {\n    s = trim(s);\n    transform(s.begin(), s.end(), s.begin(), ::tolower);\n    return s == \"true\" || s == \"1\";\n}\ninline string parseString(string s) {\n    s = trim(s);\n    if (s.length() >= 2 && s.front() == '\"' && s.back() == '\"') {\n        s = s.substr(1, s.length() - 2);\n    }\n    return s;\n}\ninline vector<int> parseVectorInt(string s) {\n    s = trim(s);\n    vector<int> res;\n    if (s.empty() || s == \"[]\") return res;\n    if (s.front() == '[') s = s.substr(1);\n    if (s.back() == ']') s = s.substr(0, s.length() - 1);\n    stringstream ss(s);\n    string token;\n    while (getline(ss, token, ',')) {\n        res.push_back(stoi(trim(token)));\n    }\n    return res;\n}\ninline vector<string> parseVectorString(string s) {\n    s = trim(s);\n    vector<string> res;\n    if (s.empty() || s == \"[]\") return res;\n    if (s.front() == '[') s = s.substr(1);\n    if (s.back() == ']') s = s.substr(0, s.length() - 1);\n    string current = \"\";\n    bool inQuotes = false;\n    for (size_t i = 0; i < s.length(); ++i) {\n        char c = s[i];\n        if (c == '\"') inQuotes = !inQuotes;\n        else if (c == ',' && !inQuotes) {\n            res.push_back(parseString(current));\n            current = \"\";\n        } else {\n            current += c;\n        }\n    }\n    if (!current.empty()) {\n        res.push_back(parseString(current));\n    }\n    return res;\n}\ninline vector<vector<int>> parseMatrixInt(string s) {\n    s = trim(s);\n    vector<vector<int>> res;\n    if (s.empty() || s == \"[]\" || s == \"[[]]\") return res;\n    if (s.front() == '[') s = s.substr(1);\n    if (s.back() == ']') s = s.substr(0, s.length() - 1);\n    string current = \"\";\n    int brackets = 0;\n    for (size_t i = 0; i < s.length(); ++i) {\n        char c = s[i];\n        if (c == '[') brackets++;\n        if (c == ']') brackets--;\n        current += c;\n        if (c == ']' && brackets == 0) {\n            res.push_back(parseVectorInt(current));\n            current = \"\";\n            if (i + 1 < s.length() && s[i+1] == ',') i++;\n        }\n    }\n    return res;\n}\ninline vector<vector<string>> parseMatrixString(string s) {\n    s = trim(s);\n    vector<vector<string>> res;\n    if (s.empty() || s == \"[]\" || s == \"[[]]\") return res;\n    if (s.front() == '[') s = s.substr(1);\n    if (s.back() == ']') s = s.substr(0, s.length() - 1);\n    string current = \"\";\n    int brackets = 0;\n    for (size_t i = 0; i < s.length(); ++i) {\n        char c = s[i];\n        if (c == '[') brackets++;\n        if (c == ']') brackets--;\n        current += c;\n        if (c == ']' && brackets == 0) {\n            res.push_back(parseVectorString(current));\n            current = \"\";\n            if (i + 1 < s.length() && s[i+1] == ',') i++;\n        }\n    }\n    return res;\n}\ninline ListNode* parseListNode(string s) {\n    vector<int> vals = parseVectorInt(s);\n    if (vals.empty()) return nullptr;\n    ListNode* head = new ListNode(vals[0]);\n    ListNode* curr = head;\n    for (size_t i = 1; i < vals.size(); ++i) {\n        curr->next = new ListNode(vals[i]);\n        curr = curr->next;\n    }\n    return head;\n}\ninline TreeNode* parseTreeNode(string s) {\n    s = trim(s);\n    if (s.empty() || s == \"[]\" || s == \"[null]\") return nullptr;\n    if (s.front() == '[') s = s.substr(1);\n    if (s.back() == ']') s = s.substr(0, s.length() - 1);\n    vector<string> tokens;\n    stringstream ss(s);\n    string token;\n    while (getline(ss, token, ',')) {\n        tokens.push_back(trim(token));\n    }\n    if (tokens.empty() || tokens[0] == \"null\") return nullptr;\n    TreeNode* root = new TreeNode(stoi(tokens[0]));\n    queue<TreeNode*> q;\n    q.push(root);\n    size_t i = 1;\n    while (!q.empty() && i < tokens.size()) {\n        TreeNode* curr = q.front();\n        q.pop();\n        if (i < tokens.size() && tokens[i] != \"null\") {\n            curr->left = new TreeNode(stoi(tokens[i]));\n            q.push(curr->left);\n        }\n        i++;\n        if (i < tokens.size() && tokens[i] != \"null\") {\n            curr->right = new TreeNode(stoi(tokens[i]));\n            q.push(curr->right);\n        }\n        i++;\n    }\n    return root;\n}\n\ninline void printResult(int val) { cout << val << endl; }\ninline void printResult(double val) { cout << val << endl; }\ninline void printResult(bool val) { cout << (val ? \"true\" : \"false\") << endl; }\ninline void printResult(string val) { cout << \"\\\"\" << val << \"\\\"\" << endl; }\ninline void printResult(vector<int> val) {\n    cout << \"[\";\n    for (size_t i = 0; i < val.size(); ++i) cout << val[i] << (i + 1 < val.size() ? \",\" : \"\");\n    cout << \"]\" << endl;\n}\ninline void printResult(vector<string> val) {\n    cout << \"[\";\n    for (size_t i = 0; i < val.size(); ++i) cout << \"\\\"\" << val[i] << \"\\\"\" << (i + 1 < val.size() ? \",\" : \"\");\n    cout << \"]\" << endl;\n}\ninline void printResult(vector<vector<int>> val) {\n    cout << \"[\";\n    for (size_t i = 0; i < val.size(); ++i) {\n        cout << \"[\";\n        for (size_t j = 0; j < val[i].size(); ++j) cout << val[i][j] << (j + 1 < val[i].size() ? \",\" : \"\");\n        cout << \"]\" << (i + 1 < val.size() ? \",\" : \"\");\n    }\n    cout << \"]\" << endl;\n}\ninline void printResult(vector<vector<string>> val) {\n    cout << \"[\";\n    for (size_t i = 0; i < val.size(); ++i) {\n        cout << \"[\";\n        for (size_t j = 0; j < val[i].size(); ++j) cout << \"\\\"\" << val[i][j] << \"\\\"\" << (j + 1 < val[i].size() ? \",\" : \"\");\n        cout << \"]\" << (i + 1 < val.size() ? \",\" : \"\");\n    }\n    cout << \"]\" << endl;\n}\ninline void printResult(ListNode* head) {\n    cout << \"[\";\n    ListNode* curr = head;\n    while (curr) {\n        cout << curr->val << (curr->next ? \",\" : \"\");\n        curr = curr->next;\n    }\n    cout << \"]\" << endl;\n}\ninline void printResult(TreeNode* root) {\n    if (!root) { cout << \"[]\" << endl; return; }\n    vector<string> res;\n    queue<TreeNode*> q;\n    q.push(root);\n    while (!q.empty()) {\n        TreeNode* curr = q.front();\n        q.pop();\n        if (curr) {\n            res.push_back(to_string(curr->val));\n            q.push(curr->left);\n            q.push(curr->right);\n        } else {\n            res.push_back(\"null\");\n        }\n    }\n    while (!res.empty() && res.back() == \"null\") res.pop_back();\n    cout << \"[\";\n    for (size_t i = 0; i < res.size(); ++i) cout << res[i] << (i + 1 < res.size() ? \",\" : \"\");\n    cout << \"]\" << endl;\n}\n\nclass TimeMap {\npublic:\n    TimeMap() {\n        \n    }\n    \n    void set(string key, string value, int timestamp) {\n        \n    }\n    \n    string get(string key, int timestamp) {\n        \n    }\n};\n\n/**\n * Your TimeMap object will be instantiated and called as such:\n * TimeMap* obj = new TimeMap();\n * obj->set(key,value,timestamp);\n * string param_2 = obj->get(key,timestamp);\n */\n\nint main() {\n    string line;\n    if (getline(cin, line)) {\n        vector<string> inputs = splitInputs(line);\n        if (inputs.size() >= 3) {\n            string key = parseString(inputs[0]);\n            string value = parseString(inputs[1]);\n            int timestamp = parseInt(inputs[2]);\n            Solution solver;\n            solver.set(key, value, timestamp);\n            printResult(key);\n        }\n    }\n    return 0;\n}\n",
      "prefix": "// --- Helper Parsing Library ---\n#include <iostream>\n#include <vector>\n#include <string>\n#include <sstream>\n#include <queue>\n#include <algorithm>\n\nusing namespace std;\n\n#ifndef CP_STRUCT_DEFS\n#define CP_STRUCT_DEFS\n// ListNode structure\nstruct ListNode {\n    int val;\n    ListNode *next;\n    ListNode() : val(0), next(nullptr) {}\n    ListNode(int x) : val(x), next(nullptr) {}\n    ListNode(int x, ListNode *next) : val(x), next(next) {}\n};\n\n// TreeNode structure\nstruct TreeNode {\n    int val;\n    TreeNode *left;\n    TreeNode *right;\n    TreeNode() : val(0), left(nullptr), right(nullptr) {}\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n    TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}\n};\n#endif\n\n// Clean string helper\ninline string trim(string s) {\n    s.erase(s.begin(), find_if(s.begin(), s.end(), [](unsigned char ch) {\n        return !isspace(ch);\n    }));\n    s.erase(find_if(s.rbegin(), s.rend(), [](unsigned char ch) {\n        return !isspace(ch);\n    }).base(), s.end());\n    return s;\n}\n\ninline vector<string> splitInputs(string line) {\n    vector<string> res;\n    string current = \"\";\n    int brackets = 0;\n    bool inQuotes = false;\n    for (size_t i = 0; i < line.length(); ++i) {\n        char c = line[i];\n        if (c == '\"' && (i == 0 || line[i-1] != '\\\\')) {\n            inQuotes = !inQuotes;\n        }\n        if (!inQuotes) {\n            if (c == '[' || c == '{') brackets++;\n            if (c == ']' || c == '}') brackets--;\n        }\n        if (c == ',' && brackets == 0 && !inQuotes) {\n            res.push_back(current);\n            current = \"\";\n        } else {\n            current += c;\n        }\n    }\n    if (!current.empty()) {\n        res.push_back(current);\n    }\n    for (auto& token : res) {\n        token = trim(token);\n        size_t eqPos = token.find('=');\n        if (eqPos != string::npos) {\n            token = trim(token.substr(eqPos + 1));\n        }\n    }\n    return res;\n}\n\ninline int parseInt(string s) { return s.empty() ? 0 : stoi(trim(s)); }\ninline double parseDouble(string s) { return s.empty() ? 0.0 : stod(trim(s)); }\ninline bool parseBool(string s) {\n    s = trim(s);\n    transform(s.begin(), s.end(), s.begin(), ::tolower);\n    return s == \"true\" || s == \"1\";\n}\ninline string parseString(string s) {\n    s = trim(s);\n    if (s.length() >= 2 && s.front() == '\"' && s.back() == '\"') {\n        s = s.substr(1, s.length() - 2);\n    }\n    return s;\n}\ninline vector<int> parseVectorInt(string s) {\n    s = trim(s);\n    vector<int> res;\n    if (s.empty() || s == \"[]\") return res;\n    if (s.front() == '[') s = s.substr(1);\n    if (s.back() == ']') s = s.substr(0, s.length() - 1);\n    stringstream ss(s);\n    string token;\n    while (getline(ss, token, ',')) {\n        res.push_back(stoi(trim(token)));\n    }\n    return res;\n}\ninline vector<string> parseVectorString(string s) {\n    s = trim(s);\n    vector<string> res;\n    if (s.empty() || s == \"[]\") return res;\n    if (s.front() == '[') s = s.substr(1);\n    if (s.back() == ']') s = s.substr(0, s.length() - 1);\n    string current = \"\";\n    bool inQuotes = false;\n    for (size_t i = 0; i < s.length(); ++i) {\n        char c = s[i];\n        if (c == '\"') inQuotes = !inQuotes;\n        else if (c == ',' && !inQuotes) {\n            res.push_back(parseString(current));\n            current = \"\";\n        } else {\n            current += c;\n        }\n    }\n    if (!current.empty()) {\n        res.push_back(parseString(current));\n    }\n    return res;\n}\ninline vector<vector<int>> parseMatrixInt(string s) {\n    s = trim(s);\n    vector<vector<int>> res;\n    if (s.empty() || s == \"[]\" || s == \"[[]]\") return res;\n    if (s.front() == '[') s = s.substr(1);\n    if (s.back() == ']') s = s.substr(0, s.length() - 1);\n    string current = \"\";\n    int brackets = 0;\n    for (size_t i = 0; i < s.length(); ++i) {\n        char c = s[i];\n        if (c == '[') brackets++;\n        if (c == ']') brackets--;\n        current += c;\n        if (c == ']' && brackets == 0) {\n            res.push_back(parseVectorInt(current));\n            current = \"\";\n            if (i + 1 < s.length() && s[i+1] == ',') i++;\n        }\n    }\n    return res;\n}\ninline vector<vector<string>> parseMatrixString(string s) {\n    s = trim(s);\n    vector<vector<string>> res;\n    if (s.empty() || s == \"[]\" || s == \"[[]]\") return res;\n    if (s.front() == '[') s = s.substr(1);\n    if (s.back() == ']') s = s.substr(0, s.length() - 1);\n    string current = \"\";\n    int brackets = 0;\n    for (size_t i = 0; i < s.length(); ++i) {\n        char c = s[i];\n        if (c == '[') brackets++;\n        if (c == ']') brackets--;\n        current += c;\n        if (c == ']' && brackets == 0) {\n            res.push_back(parseVectorString(current));\n            current = \"\";\n            if (i + 1 < s.length() && s[i+1] == ',') i++;\n        }\n    }\n    return res;\n}\ninline vector<string> splitTopLevelBrackets(string s) {\n    s = trim(s);\n    vector<string> res;\n    if (s.empty() || s == \"[]\") return res;\n    if (s.front() == '[') s = s.substr(1);\n    if (s.back() == ']') s = s.substr(0, s.length() - 1);\n    string current = \"\";\n    int brackets = 0;\n    for (size_t i = 0; i < s.length(); ++i) {\n        char c = s[i];\n        if (c == '[') brackets++;\n        if (c == ']') brackets--;\n        current += c;\n        if (c == ']' && brackets == 0) {\n            res.push_back(current);\n            current = \"\";\n            if (i + 1 < s.length() && s[i+1] == ',') i++;\n        }\n    }\n    return res;\n}\ninline ListNode* parseListNode(string s) {\n    vector<int> vals = parseVectorInt(s);\n    if (vals.empty()) return nullptr;\n    ListNode* head = new ListNode(vals[0]);\n    ListNode* curr = head;\n    for (size_t i = 1; i < vals.size(); ++i) {\n        curr->next = new ListNode(vals[i]);\n        curr = curr->next;\n    }\n    return head;\n}\ninline TreeNode* parseTreeNode(string s) {\n    s = trim(s);\n    if (s.empty() || s == \"[]\" || s == \"[null]\") return nullptr;\n    if (s.front() == '[') s = s.substr(1);\n    if (s.back() == ']') s = s.substr(0, s.length() - 1);\n    vector<string> tokens;\n    stringstream ss(s);\n    string token;\n    while (getline(ss, token, ',')) {\n        tokens.push_back(trim(token));\n    }\n    if (tokens.empty() || tokens[0] == \"null\") return nullptr;\n    TreeNode* root = new TreeNode(stoi(tokens[0]));\n    queue<TreeNode*> q;\n    q.push(root);\n    size_t i = 1;\n    while (!q.empty() && i < tokens.size()) {\n        TreeNode* curr = q.front();\n        q.pop();\n        if (i < tokens.size() && tokens[i] != \"null\") {\n            curr->left = new TreeNode(stoi(tokens[i]));\n            q.push(curr->left);\n        }\n        i++;\n        if (i < tokens.size() && tokens[i] != \"null\") {\n            curr->right = new TreeNode(stoi(tokens[i]));\n            q.push(curr->right);\n        }\n        i++;\n    }\n    return root;\n}\n\ninline void printResult(int val) { cout << val << endl; }\ninline void printResult(double val) { cout << val << endl; }\ninline void printResult(bool val) { cout << (val ? \"true\" : \"false\") << endl; }\ninline void printResult(string val) { cout << \"\\\"\" << val << \"\\\"\" << endl; }\ninline void printResult(vector<int> val) {\n    cout << \"[\";\n    for (size_t i = 0; i < val.size(); ++i) cout << val[i] << (i + 1 < val.size() ? \",\" : \"\");\n    cout << \"]\" << endl;\n}\ninline void printResult(vector<string> val) {\n    cout << \"[\";\n    for (size_t i = 0; i < val.size(); ++i) cout << \"\\\"\" << val[i] << \"\\\"\" << (i + 1 < val.size() ? \",\" : \"\");\n    cout << \"]\" << endl;\n}\ninline void printResult(vector<vector<int>> val) {\n    cout << \"[\";\n    for (size_t i = 0; i < val.size(); ++i) {\n        cout << \"[\";\n        for (size_t j = 0; j < val[i].size(); ++j) cout << val[i][j] << (j + 1 < val[i].size() ? \",\" : \"\");\n        cout << \"]\" << (i + 1 < val.size() ? \",\" : \"\");\n    }\n    cout << \"]\" << endl;\n}\ninline void printResult(vector<vector<string>> val) {\n    cout << \"[\";\n    for (size_t i = 0; i < val.size(); ++i) {\n        cout << \"[\";\n        for (size_t j = 0; j < val[i].size(); ++j) cout << \"\\\"\" << val[i][j] << \"\\\"\" << (j + 1 < val[i].size() ? \",\" : \"\");\n        cout << \"]\" << (i + 1 < val.size() ? \",\" : \"\");\n    }\n    cout << \"]\" << endl;\n}\ninline void printResult(ListNode* head) {\n    cout << \"[\";\n    ListNode* curr = head;\n    while (curr) {\n        cout << curr->val << (curr->next ? \",\" : \"\");\n        curr = curr->next;\n    }\n    cout << \"]\" << endl;\n}\ninline void printResult(TreeNode* root) {\n    if (!root) { cout << \"[]\" << endl; return; }\n    vector<string> res;\n    queue<TreeNode*> q;\n    q.push(root);\n    while (!q.empty()) {\n        TreeNode* curr = q.front();\n        q.pop();\n        if (curr) {\n            res.push_back(to_string(curr->val));\n            q.push(curr->left);\n            q.push(curr->right);\n        } else {\n            res.push_back(\"null\");\n        }\n    }\n    while (!res.empty() && res.back() == \"null\") res.pop_back();\n    cout << \"[\";\n    for (size_t i = 0; i < res.size(); ++i) cout << res[i] << (i + 1 < res.size() ? \",\" : \"\");\n    cout << \"]\" << endl;\n}\n\n",
      "body": "class TimeMap {\npublic:\n    TimeMap() {\n        // Write your solution here\n    }\n    \n    void set(string key, string value, int timestamp) {\n        // Write your solution here\n    }\n    \n    string get(string key, int timestamp) {\n        // Write your solution here\n        return {};\n    }\n};",
      "suffix": "\nint main() {\n    string line;\n    if (getline(cin, line)) {\n        vector<string> inputs = splitInputs(line);\n        if (inputs.size() >= 2) {\n            vector<string> ops = parseVectorString(inputs[0]);\n            vector<string> argBlocks = splitTopLevelBrackets(inputs[1]);\n            auto innerOf = [](const string& blk) -> string {\n                if (blk.length() >= 2) return blk.substr(1, blk.length() - 2);\n                return string();\n            };\n            vector<string> ctorToks = argBlocks.empty() ? vector<string>() : splitInputs(innerOf(argBlocks[0]));\n            TimeMap* obj = new TimeMap();\n            vector<string> results;\n            results.push_back(\"null\");\n            for (size_t i = 1; i < ops.size() && i < argBlocks.size(); ++i) {\n                string inner = innerOf(argBlocks[i]);\n                vector<string> a = inner.empty() ? vector<string>() : splitInputs(inner);\n                if (ops[i] == \"set\") {\n                    auto arg0 = parseString(a[0]);\n                    auto arg1 = parseString(a[1]);\n                    auto arg2 = parseInt(a[2]);\n                    obj->set(arg0, arg1, arg2);\n                    results.push_back(\"null\");\n                }\n                else if (ops[i] == \"get\") {\n                    auto arg0 = parseString(a[0]);\n                    auto arg1 = parseInt(a[1]);\n                    auto r = obj->get(arg0, arg1);\n                    results.push_back((\"\\\"\" + (r) + \"\\\"\"));\n                }\n            }\n            cout << \"[\";\n            for (size_t i = 0; i < results.size(); ++i) cout << results[i] << (i + 1 < results.size() ? \",\" : \"\");\n            cout << \"]\" << endl;\n        }\n    }\n    return 0;\n}\n"
    },
    {
      "language": "Java",
      "lang_slug": "java",
      "code": "// --- Helper Parsing Library ---\nimport java.io.*;\nimport java.util.*;\n\nclass ListNode {\n    int val;\n    ListNode next;\n    ListNode() {}\n    ListNode(int val) { this.val = val; }\n    ListNode(int val, ListNode next) { this.val = val; this.next = next; }\n}\n\nclass TreeNode {\n    int val;\n    TreeNode left;\n    TreeNode right;\n    TreeNode() {}\n    TreeNode(int val) { this.val = val; }\n    TreeNode(int val, TreeNode left, TreeNode right) {\n        this.val = val;\n        this.left = left;\n        this.right = right;\n    }\n}\n\nclass Parser {\n    public static String trim(String s) {\n        return s.trim();\n    }\n    \n    public static List<String> splitInputs(String line) {\n        List<String> res = new ArrayList<>();\n        StringBuilder current = new StringBuilder();\n        int brackets = 0;\n        boolean inQuotes = false;\n        \n        for (int i = 0; i < line.length(); ++i) {\n            char c = line.charAt(i);\n            if (c == '\"' && (i == 0 || line.charAt(i-1) != '\\\\')) {\n                inQuotes = !inQuotes;\n            }\n            if (!inQuotes) {\n                if (c == '[' || c == '{') brackets++;\n                if (c == ']' || c == '}') brackets--;\n            }\n            \n            if (c == ',' && brackets == 0 && !inQuotes) {\n                res.add(current.toString());\n                current = new StringBuilder();\n            } else {\n                current.append(c);\n            }\n        }\n        if (current.length() > 0) {\n            res.add(current.toString());\n        }\n        \n        List<String> cleaned = new ArrayList<>();\n        for (String token : res) {\n            token = trim(token);\n            int eqPos = token.indexOf('=');\n            if (eqPos != -1) {\n                token = trim(token.substring(eqPos + 1));\n            }\n            cleaned.add(token);\n        }\n        return cleaned;\n    }\n    \n    public static int parseInt(String s) {\n        return s.isEmpty() ? 0 : Integer.parseInt(trim(s));\n    }\n    \n    public static double parseDouble(String s) {\n        return s.isEmpty() ? 0.0 : Double.parseDouble(trim(s));\n    }\n    \n    public static boolean parseBool(String s) {\n        s = trim(s).toLowerCase();\n        return s.equals(\"true\") || s.equals(\"1\");\n    }\n    \n    public static String parseString(String s) {\n        s = trim(s);\n        if (s.length() >= 2 && s.startsWith(\"\\\"\") && s.endsWith(\"\\\"\")) {\n            s = s.substring(1, s.length() - 1);\n        }\n        return s;\n    }\n    \n    public static int[] parseArrayInt(String s) {\n        s = trim(s);\n        if (s.isEmpty() || s.equals(\"[]\")) return new int[0];\n        if (s.startsWith(\"[\")) s = s.substring(1);\n        if (s.endsWith(\"]\")) s = s.substring(0, s.length() - 1);\n        String[] tokens = s.split(\",\");\n        int[] res = new int[tokens.length];\n        for (int i = 0; i < tokens.length; i++) {\n            res[i] = Integer.parseInt(trim(tokens[i]));\n        }\n        return res;\n    }\n\n    public static List<Integer> parseListInteger(String s) {\n        int[] arr = parseArrayInt(s);\n        List<Integer> res = new ArrayList<>();\n        for (int x : arr) res.add(x);\n        return res;\n    }\n    \n    public static String[] parseArrayString(String s) {\n        s = trim(s);\n        if (s.isEmpty() || s.equals(\"[]\")) return new String[0];\n        if (s.startsWith(\"[\")) s = s.substring(1);\n        if (s.endsWith(\"]\")) s = s.substring(0, s.length() - 1);\n        \n        List<String> res = new ArrayList<>();\n        StringBuilder current = new StringBuilder();\n        boolean inQuotes = false;\n        for (int i = 0; i < s.length(); ++i) {\n            char c = s.charAt(i);\n            if (c == '\"') inQuotes = !inQuotes;\n            else if (c == ',' && !inQuotes) {\n                res.add(parseString(current.toString()));\n                current = new StringBuilder();\n            } else {\n                current.append(c);\n            }\n        }\n        if (current.length() > 0) {\n            res.add(parseString(current.toString()));\n        }\n        return res.toArray(new String[0]);\n    }\n\n    public static List<String> parseListString(String s) {\n        return Arrays.asList(parseArrayString(s));\n    }\n    \n    public static int[][] parseMatrixInt(String s) {\n        s = trim(s);\n        if (s.isEmpty() || s.equals(\"[]\") || s.equals(\"[[]]\")) return new int[0][0];\n        if (s.startsWith(\"[\")) s = s.substring(1);\n        if (s.endsWith(\"]\")) s = s.substring(0, s.length() - 1);\n        \n        List<int[]> res = new ArrayList<>();\n        StringBuilder current = new StringBuilder();\n        int brackets = 0;\n        for (int i = 0; i < s.length(); ++i) {\n            char c = s.charAt(i);\n            if (c == '[') brackets++;\n            if (c == ']') brackets--;\n            current.append(c);\n            if (c == ']' && brackets == 0) {\n                res.add(parseArrayInt(current.toString()));\n                current = new StringBuilder();\n                if (i + 1 < s.length() && s.charAt(i+1) == ',') i++;\n            }\n        }\n        return res.toArray(new int[0][0]);\n    }\n\n    public static List<List<Integer>> parseListListInteger(String s) {\n        int[][] mat = parseMatrixInt(s);\n        List<List<Integer>> res = new ArrayList<>();\n        for (int[] row : mat) {\n            List<Integer> r = new ArrayList<>();\n            for (int x : row) r.add(x);\n            res.add(r);\n        }\n        return res;\n    }\n    \n    public static List<List<String>> parseListListString(String s) {\n        s = trim(s);\n        if (s.isEmpty() || s.equals(\"[]\") || s.equals(\"[[]]\")) return new ArrayList<>();\n        if (s.startsWith(\"[\")) s = s.substring(1);\n        if (s.endsWith(\"]\")) s = s.substring(0, s.length() - 1);\n        \n        List<List<String>> res = new ArrayList<>();\n        StringBuilder current = new StringBuilder();\n        int brackets = 0;\n        for (int i = 0; i < s.length(); ++i) {\n            char c = s.charAt(i);\n            if (c == '[') brackets++;\n            if (c == ']') brackets--;\n            current.append(c);\n            if (c == ']' && brackets == 0) {\n                res.add(parseListString(current.toString()));\n                current = new StringBuilder();\n                if (i + 1 < s.length() && s.charAt(i+1) == ',') i++;\n            }\n        }\n        return res;\n    }\n    \n    public static ListNode parseListNode(String s) {\n        int[] vals = parseArrayInt(s);\n        if (vals.length == 0) return null;\n        ListNode head = new ListNode(vals[0]);\n        ListNode curr = head;\n        for (int i = 1; i < vals.length; ++i) {\n            curr.next = new ListNode(vals[i]);\n            curr = curr.next;\n        }\n        return head;\n    }\n    \n    public static TreeNode parseTreeNode(String s) {\n        s = trim(s);\n        if (s.isEmpty() || s.equals(\"[]\") || s.equals(\"[null]\")) return null;\n        if (s.startsWith(\"[\")) s = s.substring(1);\n        if (s.endsWith(\"]\")) s = s.substring(0, s.length() - 1);\n        \n        String[] tokens = s.split(\",\");\n        for (int i = 0; i < tokens.length; i++) tokens[i] = trim(tokens[i]);\n        \n        if (tokens.length == 0 || tokens[0].equals(\"null\")) return null;\n        \n        TreeNode root = new TreeNode(Integer.parseInt(tokens[0]));\n        Queue<TreeNode> q = new LinkedList<>();\n        q.add(root);\n        \n        int i = 1;\n        while (!q.isEmpty() && i < tokens.length) {\n            TreeNode curr = q.poll();\n            \n            if (i < tokens.length && !tokens[i].equals(\"null\")) {\n                curr.left = new TreeNode(Integer.parseInt(tokens[i]));\n                q.add(curr.left);\n            }\n            i++;\n            \n            if (i < tokens.length && !tokens[i].equals(\"null\")) {\n                curr.right = new TreeNode(Integer.parseInt(tokens[i]));\n                q.add(curr.right);\n            }\n            i++;\n        }\n        return root;\n    }\n}\n\nclass Printer {\n    public static void print(int val) { System.out.println(val); }\n    public static void print(double val) { System.out.println(val); }\n    public static void print(boolean val) { System.out.println(val); }\n    public static void print(String val) { System.out.println(\"\\\"\" + val + \"\\\"\"); }\n    \n    public static void print(int[] val) {\n        System.out.print(\"[\");\n        for (int i = 0; i < val.length; ++i) {\n            System.out.print(val[i] + (i + 1 < val.length ? \",\" : \"\"));\n        }\n        System.out.println(\"]\");\n    }\n    \n    public static void print(String[] val) {\n        System.out.print(\"[\");\n        for (int i = 0; i < val.length; ++i) {\n            System.out.print(\"\\\"\" + val[i] + \"\\\"\" + (i + 1 < val.length ? \",\" : \"\"));\n        }\n        System.out.println(\"]\");\n    }\n    \n    public static void print(List<?> val) {\n        System.out.println(new ArrayList<>(val).toString().replace(\" \", \"\"));\n    }\n    \n    public static void print(ListNode head) {\n        System.out.print(\"[\");\n        ListNode curr = head;\n        while (curr != null) {\n            System.out.print(curr.val + (curr.next != null ? \",\" : \"\"));\n            curr = curr.next;\n        }\n        System.out.println(\"]\");\n    }\n    \n    public static void print(TreeNode root) {\n        if (root == null) {\n            System.out.println(\"[]\");\n            return;\n        }\n        List<String> res = new ArrayList<>();\n        Queue<TreeNode> q = new LinkedList<>();\n        q.add(root);\n        while (!q.isEmpty()) {\n            TreeNode curr = q.poll();\n            if (curr != null) {\n                res.add(String.valueOf(curr.val));\n                q.add(curr.left);\n                q.add(curr.right);\n            } else {\n                res.add(\"null\");\n            }\n        }\n        while (!res.isEmpty() && res.get(res.size() - 1).equals(\"null\")) {\n            res.remove(res.size() - 1);\n        }\n        System.out.print(\"[\");\n        for (int i = 0; i < res.size(); ++i) {\n            System.out.print(res.get(i) + (i + 1 < res.size() ? \",\" : \"\"));\n        }\n        System.out.println(\"]\");\n    }\n}\n\nclass TimeMap {\n\n    public TimeMap() {\n        \n    }\n    \n    public void set(String key, String value, int timestamp) {\n        \n    }\n    \n    public String get(String key, int timestamp) {\n        \n    }\n}\n\n/**\n * Your TimeMap object will be instantiated and called as such:\n * TimeMap obj = new TimeMap();\n * obj.set(key,value,timestamp);\n * String param_2 = obj.get(key,timestamp);\n */\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        String line = br.readLine();\n        if (line != null) {\n            List<String> inputs = Parser.splitInputs(line);\n            if (inputs.size() >= 3) {\n                String key = Parser.parseString(inputs.get(0));\n            String value = Parser.parseString(inputs.get(1));\n            int timestamp = Parser.parseInt(inputs.get(2));\n                Solution solver = new Solution();\n                solver.set(key, value, timestamp);\n            Printer.print(key);\n            }\n        }\n    }\n}\n",
      "prefix": "// --- Helper Parsing Library ---\nimport java.io.*;\nimport java.util.*;\n\nclass ListNode {\n    int val;\n    ListNode next;\n    ListNode() {}\n    ListNode(int val) { this.val = val; }\n    ListNode(int val, ListNode next) { this.val = val; this.next = next; }\n}\n\nclass TreeNode {\n    int val;\n    TreeNode left;\n    TreeNode right;\n    TreeNode() {}\n    TreeNode(int val) { this.val = val; }\n    TreeNode(int val, TreeNode left, TreeNode right) {\n        this.val = val;\n        this.left = left;\n        this.right = right;\n    }\n}\n\nclass Parser {\n    public static String trim(String s) {\n        return s.trim();\n    }\n    \n    public static List<String> splitInputs(String line) {\n        List<String> res = new ArrayList<>();\n        StringBuilder current = new StringBuilder();\n        int brackets = 0;\n        boolean inQuotes = false;\n        \n        for (int i = 0; i < line.length(); ++i) {\n            char c = line.charAt(i);\n            if (c == '\"' && (i == 0 || line.charAt(i-1) != '\\\\')) {\n                inQuotes = !inQuotes;\n            }\n            if (!inQuotes) {\n                if (c == '[' || c == '{') brackets++;\n                if (c == ']' || c == '}') brackets--;\n            }\n            \n            if (c == ',' && brackets == 0 && !inQuotes) {\n                res.add(current.toString());\n                current = new StringBuilder();\n            } else {\n                current.append(c);\n            }\n        }\n        if (current.length() > 0) {\n            res.add(current.toString());\n        }\n        \n        List<String> cleaned = new ArrayList<>();\n        for (String token : res) {\n            token = trim(token);\n            int eqPos = token.indexOf('=');\n            if (eqPos != -1) {\n                token = trim(token.substring(eqPos + 1));\n            }\n            cleaned.add(token);\n        }\n        return cleaned;\n    }\n    \n    public static int parseInt(String s) {\n        return s.isEmpty() ? 0 : Integer.parseInt(trim(s));\n    }\n    \n    public static double parseDouble(String s) {\n        return s.isEmpty() ? 0.0 : Double.parseDouble(trim(s));\n    }\n    \n    public static boolean parseBool(String s) {\n        s = trim(s).toLowerCase();\n        return s.equals(\"true\") || s.equals(\"1\");\n    }\n    \n    public static String parseString(String s) {\n        s = trim(s);\n        if (s.length() >= 2 && s.startsWith(\"\\\"\") && s.endsWith(\"\\\"\")) {\n            s = s.substring(1, s.length() - 1);\n        }\n        return s;\n    }\n    \n    public static int[] parseArrayInt(String s) {\n        s = trim(s);\n        if (s.isEmpty() || s.equals(\"[]\")) return new int[0];\n        if (s.startsWith(\"[\")) s = s.substring(1);\n        if (s.endsWith(\"]\")) s = s.substring(0, s.length() - 1);\n        String[] tokens = s.split(\",\");\n        int[] res = new int[tokens.length];\n        for (int i = 0; i < tokens.length; i++) {\n            res[i] = Integer.parseInt(trim(tokens[i]));\n        }\n        return res;\n    }\n\n    public static List<Integer> parseListInteger(String s) {\n        int[] arr = parseArrayInt(s);\n        List<Integer> res = new ArrayList<>();\n        for (int x : arr) res.add(x);\n        return res;\n    }\n    \n    public static String[] parseArrayString(String s) {\n        s = trim(s);\n        if (s.isEmpty() || s.equals(\"[]\")) return new String[0];\n        if (s.startsWith(\"[\")) s = s.substring(1);\n        if (s.endsWith(\"]\")) s = s.substring(0, s.length() - 1);\n        \n        List<String> res = new ArrayList<>();\n        StringBuilder current = new StringBuilder();\n        boolean inQuotes = false;\n        for (int i = 0; i < s.length(); ++i) {\n            char c = s.charAt(i);\n            if (c == '\"') inQuotes = !inQuotes;\n            else if (c == ',' && !inQuotes) {\n                res.add(parseString(current.toString()));\n                current = new StringBuilder();\n            } else {\n                current.append(c);\n            }\n        }\n        if (current.length() > 0) {\n            res.add(parseString(current.toString()));\n        }\n        return res.toArray(new String[0]);\n    }\n\n    public static List<String> parseListString(String s) {\n        return Arrays.asList(parseArrayString(s));\n    }\n    \n    public static int[][] parseMatrixInt(String s) {\n        s = trim(s);\n        if (s.isEmpty() || s.equals(\"[]\") || s.equals(\"[[]]\")) return new int[0][0];\n        if (s.startsWith(\"[\")) s = s.substring(1);\n        if (s.endsWith(\"]\")) s = s.substring(0, s.length() - 1);\n        \n        List<int[]> res = new ArrayList<>();\n        StringBuilder current = new StringBuilder();\n        int brackets = 0;\n        for (int i = 0; i < s.length(); ++i) {\n            char c = s.charAt(i);\n            if (c == '[') brackets++;\n            if (c == ']') brackets--;\n            current.append(c);\n            if (c == ']' && brackets == 0) {\n                res.add(parseArrayInt(current.toString()));\n                current = new StringBuilder();\n                if (i + 1 < s.length() && s.charAt(i+1) == ',') i++;\n            }\n        }\n        return res.toArray(new int[0][0]);\n    }\n\n    public static List<List<Integer>> parseListListInteger(String s) {\n        int[][] mat = parseMatrixInt(s);\n        List<List<Integer>> res = new ArrayList<>();\n        for (int[] row : mat) {\n            List<Integer> r = new ArrayList<>();\n            for (int x : row) r.add(x);\n            res.add(r);\n        }\n        return res;\n    }\n    \n    public static List<List<String>> parseListListString(String s) {\n        s = trim(s);\n        if (s.isEmpty() || s.equals(\"[]\") || s.equals(\"[[]]\")) return new ArrayList<>();\n        if (s.startsWith(\"[\")) s = s.substring(1);\n        if (s.endsWith(\"]\")) s = s.substring(0, s.length() - 1);\n        \n        List<List<String>> res = new ArrayList<>();\n        StringBuilder current = new StringBuilder();\n        int brackets = 0;\n        for (int i = 0; i < s.length(); ++i) {\n            char c = s.charAt(i);\n            if (c == '[') brackets++;\n            if (c == ']') brackets--;\n            current.append(c);\n            if (c == ']' && brackets == 0) {\n                res.add(parseListString(current.toString()));\n                current = new StringBuilder();\n                if (i + 1 < s.length() && s.charAt(i+1) == ',') i++;\n            }\n        }\n        return res;\n    }\n    \n    public static String innerOf(String blk) {\n        if (blk.length() >= 2) return blk.substring(1, blk.length() - 1);\n        return \"\";\n    }\n    \n    public static List<String> splitTopLevelBrackets(String s) {\n        s = trim(s);\n        List<String> res = new ArrayList<>();\n        if (s.isEmpty() || s.equals(\"[]\")) return res;\n        if (s.startsWith(\"[\")) s = s.substring(1);\n        if (s.endsWith(\"]\")) s = s.substring(0, s.length() - 1);\n        StringBuilder current = new StringBuilder();\n        int brackets = 0;\n        for (int i = 0; i < s.length(); ++i) {\n            char c = s.charAt(i);\n            if (c == '[') brackets++;\n            if (c == ']') brackets--;\n            current.append(c);\n            if (c == ']' && brackets == 0) {\n                res.add(current.toString());\n                current = new StringBuilder();\n                if (i + 1 < s.length() && s.charAt(i + 1) == ',') i++;\n            }\n        }\n        return res;\n    }\n    \n    public static ListNode parseListNode(String s) {\n        int[] vals = parseArrayInt(s);\n        if (vals.length == 0) return null;\n        ListNode head = new ListNode(vals[0]);\n        ListNode curr = head;\n        for (int i = 1; i < vals.length; ++i) {\n            curr.next = new ListNode(vals[i]);\n            curr = curr.next;\n        }\n        return head;\n    }\n    \n    public static TreeNode parseTreeNode(String s) {\n        s = trim(s);\n        if (s.isEmpty() || s.equals(\"[]\") || s.equals(\"[null]\")) return null;\n        if (s.startsWith(\"[\")) s = s.substring(1);\n        if (s.endsWith(\"]\")) s = s.substring(0, s.length() - 1);\n        \n        String[] tokens = s.split(\",\");\n        for (int i = 0; i < tokens.length; i++) tokens[i] = trim(tokens[i]);\n        \n        if (tokens.length == 0 || tokens[0].equals(\"null\")) return null;\n        \n        TreeNode root = new TreeNode(Integer.parseInt(tokens[0]));\n        Queue<TreeNode> q = new LinkedList<>();\n        q.add(root);\n        \n        int i = 1;\n        while (!q.isEmpty() && i < tokens.length) {\n            TreeNode curr = q.poll();\n            \n            if (i < tokens.length && !tokens[i].equals(\"null\")) {\n                curr.left = new TreeNode(Integer.parseInt(tokens[i]));\n                q.add(curr.left);\n            }\n            i++;\n            \n            if (i < tokens.length && !tokens[i].equals(\"null\")) {\n                curr.right = new TreeNode(Integer.parseInt(tokens[i]));\n                q.add(curr.right);\n            }\n            i++;\n        }\n        return root;\n    }\n}\n\nclass Printer {\n    public static void print(int val) { System.out.println(val); }\n    public static void print(double val) { System.out.println(val); }\n    public static void print(boolean val) { System.out.println(val); }\n    public static void print(String val) { System.out.println(\"\\\"\" + val + \"\\\"\"); }\n    \n    public static void print(int[] val) {\n        System.out.print(\"[\");\n        for (int i = 0; i < val.length; ++i) {\n            System.out.print(val[i] + (i + 1 < val.length ? \",\" : \"\"));\n        }\n        System.out.println(\"]\");\n    }\n    \n    public static void print(String[] val) {\n        System.out.print(\"[\");\n        for (int i = 0; i < val.length; ++i) {\n            System.out.print(\"\\\"\" + val[i] + \"\\\"\" + (i + 1 < val.length ? \",\" : \"\"));\n        }\n        System.out.println(\"]\");\n    }\n    \n    public static void print(List<?> val) {\n        System.out.println(new ArrayList<>(val).toString().replace(\" \", \"\"));\n    }\n    \n    public static void print(ListNode head) {\n        System.out.print(\"[\");\n        ListNode curr = head;\n        while (curr != null) {\n            System.out.print(curr.val + (curr.next != null ? \",\" : \"\"));\n            curr = curr.next;\n        }\n        System.out.println(\"]\");\n    }\n    \n    public static void print(TreeNode root) {\n        if (root == null) {\n            System.out.println(\"[]\");\n            return;\n        }\n        List<String> res = new ArrayList<>();\n        Queue<TreeNode> q = new LinkedList<>();\n        q.add(root);\n        while (!q.isEmpty()) {\n            TreeNode curr = q.poll();\n            if (curr != null) {\n                res.add(String.valueOf(curr.val));\n                q.add(curr.left);\n                q.add(curr.right);\n            } else {\n                res.add(\"null\");\n            }\n        }\n        while (!res.isEmpty() && res.get(res.size() - 1).equals(\"null\")) {\n            res.remove(res.size() - 1);\n        }\n        System.out.print(\"[\");\n        for (int i = 0; i < res.size(); ++i) {\n            System.out.print(res.get(i) + (i + 1 < res.size() ? \",\" : \"\"));\n        }\n        System.out.println(\"]\");\n    }\n}\n\n",
      "body": "class TimeMap {\n\n    public TimeMap() {\n        // Write your solution here\n    }\n    \n    public void set(String key, String value, int timestamp) {\n        // Write your solution here\n    }\n    \n    public String get(String key, int timestamp) {\n        // Write your solution here\n        return \"\";\n    }\n}",
      "suffix": "\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        String line = br.readLine();\n        if (line != null) {\n            List<String> inputs = Parser.splitInputs(line);\n            if (inputs.size() >= 2) {\n                List<String> ops = Parser.parseListString(inputs.get(0));\n                List<String> argBlocks = Parser.splitTopLevelBrackets(inputs.get(1));\n                List<String> ctorToks = argBlocks.isEmpty() ? new ArrayList<>() : Parser.splitInputs(Parser.innerOf(argBlocks.get(0)));\n                TimeMap obj = new TimeMap();\n                List<String> results = new ArrayList<>();\n                results.add(\"null\");\n                for (int i = 1; i < ops.size() && i < argBlocks.size(); i++) {\n                    String inner = Parser.innerOf(argBlocks.get(i));\n                    List<String> a = inner.isEmpty() ? new ArrayList<>() : Parser.splitInputs(inner);\n                    if (ops.get(i).equals(\"set\")) {\n                        obj.set(Parser.parseString(a.get(0)), Parser.parseString(a.get(1)), Parser.parseInt(a.get(2)));\n                        results.add(\"null\");\n                    }\n                    else if (ops.get(i).equals(\"get\")) {\n                        String r = obj.get(Parser.parseString(a.get(0)), Parser.parseInt(a.get(1)));\n                        results.add((\"\\\"\" + r + \"\\\"\"));\n                    }\n                }\n                StringBuilder sb = new StringBuilder();\n                sb.append(\"[\");\n                for (int i = 0; i < results.size(); i++) sb.append(results.get(i)).append(i + 1 < results.size() ? \",\" : \"\");\n                sb.append(\"]\");\n                System.out.println(sb.toString());\n            }\n        }\n    }\n}\n"
    },
    {
      "language": "Python3",
      "lang_slug": "python3",
      "code": "# --- Helper Parsing Library ---\nimport json\nimport sys\nimport collections\n\nclass ListNode:\n    def __init__(self, val=0, next=None):\n        self.val = val\n        self.next = next\n\nclass TreeNode:\n    def __init__(self, val=0, left=None, right=None):\n        self.val = val\n        self.left = left\n        self.right = right\n\ndef parseListNode(s):\n    vals = json.loads(s)\n    if not vals: return None\n    head = ListNode(vals[0])\n    curr = head\n    for v in vals[1:]:\n        curr.next = ListNode(v)\n        curr = curr.next\n    return head\n\ndef parseTreeNode(s):\n    s = s.strip()\n    if not s or s == \"[]\" or s == \"[null]\": return None\n    if s.startswith(\"[\"): s = s[1:]\n    if s.endswith(\"]\"): s = s[:-1]\n    tokens = [t.strip() for t in s.split(',')]\n    if not tokens or tokens[0] == \"null\": return None\n    root = TreeNode(int(tokens[0]))\n    q = collections.deque([root])\n    i = 1\n    while q and i < len(tokens):\n        curr = q.popleft()\n        if i < len(tokens) and tokens[i] != \"null\":\n            curr.left = TreeNode(int(tokens[i]))\n            q.append(curr.left)\n        i += 1\n        if i < len(tokens) and tokens[i] != \"null\":\n            curr.right = TreeNode(int(tokens[i]))\n            q.append(curr.right)\n        i += 1\n    return root\n\ndef serializeListNode(head):\n    res = []\n    curr = head\n    while curr:\n        res.append(curr.val)\n        curr = curr.next\n    return json.dumps(res)\n\ndef serializeTreeNode(root):\n    if not root: return \"[]\"\n    res = []\n    q = collections.deque([root])\n    while q:\n        curr = q.popleft()\n        if curr:\n            res.append(str(curr.val))\n            q.append(curr.left)\n            q.append(curr.right)\n        else:\n            res.append(\"null\")\n    while res and res[-1] == \"null\":\n        res.pop()\n    return \"[\" + \",\".join(res) + \"]\"\n\nclass TimeMap:\n\n    def __init__(self):\n        \n\n    def set(self, key: str, value: str, timestamp: int) -> None:\n        \n\n    def get(self, key: str, timestamp: int) -> str:\n        \n\n\n# Your TimeMap object will be instantiated and called as such:\n# obj = TimeMap()\n# obj.set(key,value,timestamp)\n# param_2 = obj.get(key,timestamp)\n\nif __name__ == \"__main__\":\n    line = sys.stdin.read().strip()\n    if line:\n        inputs = []\n        brackets = 0\n        in_quotes = False\n        current = \"\"\n        for i, c in enumerate(line):\n            if c == '\"' and (i == 0 or line[i-1] != '\\\\'):\n                in_quotes = not in_quotes\n            if not in_quotes:\n                if c in ('[', '{'): brackets += 1\n                if c in (']', '}'): brackets -= 1\n            if c == ',' and brackets == 0 and not in_quotes:\n                inputs.append(current.strip())\n                current = \"\"\n            else:\n                current += c\n        if current.strip():\n            inputs.append(current.strip())\n            \n        cleaned = []\n        for token in inputs:\n            token = token.strip()\n            if '=' in token:\n                token = token.split('=', 1)[1].strip()\n            cleaned.append(token)\n            \n        if len(cleaned) >= 3:\n            key = json.loads(cleaned[0])\n            value = json.loads(cleaned[1])\n            timestamp = json.loads(cleaned[2])\n            solver = Solution()\n            solver.set(key, value, timestamp)\n            ans = str\n            print(json.dumps(ans))\n",
      "prefix": "# --- Helper Parsing Library ---\nfrom typing import List, Optional\nimport json\nimport sys\nimport collections\n\nclass ListNode:\n    def __init__(self, val=0, next=None):\n        self.val = val\n        self.next = next\n\nclass TreeNode:\n    def __init__(self, val=0, left=None, right=None):\n        self.val = val\n        self.left = left\n        self.right = right\n\ndef parseListNode(s):\n    vals = json.loads(s)\n    if not vals: return None\n    head = ListNode(vals[0])\n    curr = head\n    for v in vals[1:]:\n        curr.next = ListNode(v)\n        curr = curr.next\n    return head\n\ndef parseTreeNode(s):\n    s = s.strip()\n    if not s or s == \"[]\" or s == \"[null]\": return None\n    if s.startswith(\"[\"): s = s[1:]\n    if s.endswith(\"]\"): s = s[:-1]\n    tokens = [t.strip() for t in s.split(',')]\n    if not tokens or tokens[0] == \"null\": return None\n    root = TreeNode(int(tokens[0]))\n    q = collections.deque([root])\n    i = 1\n    while q and i < len(tokens):\n        curr = q.popleft()\n        if i < len(tokens) and tokens[i] != \"null\":\n            curr.left = TreeNode(int(tokens[i]))\n            q.append(curr.left)\n        i += 1\n        if i < len(tokens) and tokens[i] != \"null\":\n            curr.right = TreeNode(int(tokens[i]))\n            q.append(curr.right)\n        i += 1\n    return root\n\ndef serializeListNode(head):\n    res = []\n    curr = head\n    while curr:\n        res.append(curr.val)\n        curr = curr.next\n    return json.dumps(res)\n\ndef serializeTreeNode(root):\n    if not root: return \"[]\"\n    res = []\n    q = collections.deque([root])\n    while q:\n        curr = q.popleft()\n        if curr:\n            res.append(str(curr.val))\n            q.append(curr.left)\n            q.append(curr.right)\n        else:\n            res.append(\"null\")\n    while res and res[-1] == \"null\":\n        res.pop()\n    return \"[\" + \",\".join(res) + \"]\"\n\n",
      "body": "class TimeMap:\n\n    def __init__(self):\n        # Write your solution here\n        pass\n        \n\n    def set(self, key: str, value: str, timestamp: int) -> None:\n        # Write your solution here\n        pass\n        \n\n    def get(self, key: str, timestamp: int) -> str:\n        # Write your solution here\n        return \"\"\n        ",
      "suffix": "\nif __name__ == \"__main__\":\n    line = sys.stdin.read().strip()\n    if line:\n        depth = 0\n        split_idx = -1\n        for idx, ch in enumerate(line):\n            if ch == \"[\":\n                depth += 1\n            elif ch == \"]\":\n                depth -= 1\n            elif ch == \",\" and depth == 0:\n                split_idx = idx\n                break\n        if split_idx != -1:\n            ops = json.loads(line[:split_idx].strip())\n            arg_blocks = json.loads(line[split_idx + 1:].strip())\n            obj = TimeMap()\n            results = [None]\n            for i in range(1, min(len(ops), len(arg_blocks))):\n                if ops[i] == \"set\":\n                    obj.set(arg_blocks[i][0], arg_blocks[i][1], arg_blocks[i][2])\n                    results.append(None)\n                elif ops[i] == \"get\":\n                    results.append(obj.get(arg_blocks[i][0], arg_blocks[i][1]))\n            print(json.dumps(results))\n"
    }
  ]
}